{
  "name": "e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Browser tests against a running Foreman — the exit demo, as a suite",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "^1.63.0",
    "axe-core": "4.12.0"
  }
}
